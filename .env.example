# Google Gemini AI Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash-exp:generateContent

# Database Configuration (Optional - defaults are provided in docker-compose.yml)
# POSTGRES_DB=fitness_user_db
# POSTGRES_USER=root
# POSTGRES_PASSWORD=root12345

# RabbitMQ Configuration (Optional - defaults are provided in docker-compose.yml)
# RABBITMQ_DEFAULT_USER=guest
# RABBITMQ_DEFAULT_PASS=guest

# Keycloak Configuration (Optional - defaults are provided in docker-compose.yml)
# KC_BOOTSTRAP_ADMIN_USERNAME=admin
# KC_BOOTSTRAP_ADMIN_PASSWORD=admin